mutation DecryptPDF($input: DecryptPDFInputType!) {
  decryptPDF(input: $input) {
    error {
      ...ErrorFragment
    }
    success
    response {
      url
      sessionId
      sessionType
      documentName
    }
  }
}
