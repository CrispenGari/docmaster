mutation ReducePDFSize($input: ReducePDFSizeInputType!) {
  reducePDFSize(input: $input) {
    error {
      ...ErrorFragment
    }
    success
    response {
      url
      sessionId
      sessionType
      documentName
      inputSize
      outputSize
    }
  }
}
